import streamlit as st
import pandas as pd

# --- الحفاظ على هوية الرادار 42.2 الأصلية ---
st.set_page_config(layout="wide")
st.markdown("<h1 style='text-align: center;'>💎 رادار القناص V42.2 (استجابة سريعة) 💎</h1>", unsafe_allow_html=True)

# تنسيق الجداول كما في الصورة (نصوص واضحة وخلفيات ملونة)
def color_rows(val):
    if val == 'صاعد': return 'background-color: #2ecc71; color: black'
    if val == 'هابط': return 'background-color: #e74c3c; color: white'
    return ''

# البيانات مع إضافة "مؤشرات الاستباق" المطلوبة
data = {
    "إشارة": ["🔴", "🟢", "🔴", "🟢", "🔴"],
    "السهم": ["SPY", "AAPL", "NVDA", "TSLA", "MSFT"],
    "يومي %": ["+0.03%", "-0.72%", "+1.67%", "+0.89%", "-0.13%"],
    "السعر": [695.71, 256.41, 191.66, 434.73, 479.96],
    "الفلتر (الاتجاه | RSI)": ["صاعد | RSI:49", "صاعد | RSI:59", "هابط | RSI:42", "صاعد | RSI:51", "هابط | RSI:46"],
    "الجودة": ["عكس التيار ⚠️", "ممتازة ✅", "ممتازة ✅", "ممتازة ✅", "ممتازة ✅"],
    "IV": ["7.1%", "17.2%", "24.6%", "24.5%", "18.7%"],
    # --- الإضافة الجديدة (الاستباق) دون تغيير التصميم القديم ---
    "تنبيه استباقي ⚡": ["تجميع..", "انفجار وشيك 🚀", "سيولة داخلة", "ثبات..", "مراقبة"]
}

df = pd.DataFrame(data)

# عرض الجدول بنفس التنسيق الذي تحبه
st.table(df)

# دليل القناص الجانبي (كما في الصورة)
st.sidebar.header("دليل القناص")
st.sidebar.markdown("""
- ادخل مع التيار: ممتازة ✅
- مخاطرة عالية: عكس التيار ⚠️
- تشبع شرائي: RSI > 70
- تشبع بيعي: RSI < 30
""")
